# secrets — never commit
.env

# python
__pycache__/
*.pyc
*.pyo
.DS_Store

# next.js build output — Vercel builds this itself
web/.next/
web/node_modules/

# old db (deleted, but just in case)
data/*.db
